import React from 'react';
import {Button, View} from 'react-native-ui-lib';

const BottomBar = () => {
  return (
    <View>
      <Button>Home</Button>
    </View>
  );
};

export default BottomBar;
